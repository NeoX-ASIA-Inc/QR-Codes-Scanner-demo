class IconStateInfo {
  bool isIcon1Red = false;
  bool isIcon2Blue = false;
  bool isButtonEnabled = false;

  IconStateInfo({required this.isButtonEnabled, required this.isIcon1Red, required this.isIcon2Blue});
}